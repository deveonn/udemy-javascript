// String, Number, Boolean, Null, Undefined

// Strings
const nome = 'Luiz'
const nome1 = "Luiz"
const nome2 = `Luiz`

// Number
const num1 = 10
const num2 = 10.50

/**
 Quando declaramos a variavel sem atribuir nada, ela tem o valor de 'undefined'
 undefined é um tipo primitivo, exemplo:
 */

let nomeAluno; //indefinido -> não aponta para lugar nenhum na memoria
let sobrenomeAluno = null //nulo -> não aponta para lugar nenhum na memoria

// Usamos null, por exemplo, para 'resetar' o valor de uma variável dentro uma aplicação se necessário.

// Boolean(true ou false), valor lógico
const boolean = true

// Sempre que usamos booleans, queremos fazer alguma mudança de fluxo na aplicação, se isso for true faça isso, se não, faça outra coisa ...